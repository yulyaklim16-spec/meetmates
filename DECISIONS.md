# Журнал решений

> История продукта: что решили, когда, почему и какой ценой.
> **Спецификация — в [`CLAUDE.md`](./CLAUDE.md)**, она отвечает на вопрос «как устроен продукт сейчас».
> Этот файл отвечает на вопрос «почему так» и нужен, когда решение хочется пересмотреть.
>
> Правило: в спецификации стоит ссылка `D-NN`, обоснование живёт здесь. Одно решение — одна строка.
> Отменённые решения не стираются: у них проставлено, чем и когда они сняты.

---

## Как читать

| Поле | Что значит |
|---|---|
| **Решение** | что стало правдой о продукте |
| **Где** | раздел брифа, в котором это описано |
| **Почему** | одно основание, а не пересказ разбора |
| **Цена** | чего решение не даёт или чем за него заплачено |

**Уровни источников:** **A** рецензируемое и официальное · **B** коммерческая аналитика ·
**C** блоги и пересказы · `[?]` источника нет.

---

## 2026-09-07 — бриф утверждён

| # | Решение | Где | Почему | Цена |
|---|---|---|---|---|
| **D-01** | Этап — кликабельный дизайн-прототип на мок-данных, бэкенда нет | §3 | Проверять флоу дешевле, чем строить | Поведенческих данных не будет до теста |
| **D-02** | Сегмент остаётся широким: 18–35, все интересы | §2.1 | Сужение до переехавших отклонено как преждевременное | Это конфигурация, на которой закрылся Ramle и пустует киевский Meetup. Риск компенсируется дизайном |
| **D-03** | Верификация обязательна, но гейт стоит **перед контактом**, а не на входе | §7.3 | На онбординге (модель Peanut) убивает активацию при пустой базе | Часть людей упрётся в гейт уже после матча |
| **D-04** | Целевая модель выручки — комиссия с платных событий | §9 | Единственная модель, растущая вместе с состоявшимися встречами | ~~действует~~ → снято **D-28** |

## 2026-09-08 — после прохода внутрь Tinder и Meetup

| # | Решение | Где | Почему | Цена |
|---|---|---|---|---|
| **D-05** | «Это НЕ дейтинг» — проверяемая гарантия, а не главное обещание | §1 | Tinder уже продаёт дружбу: «Найти друзей» на шаге намерения, плитка в Explore. Отрицательное обещание против него работает на него | Отстройка переезжает на единицу и репутацию — их надо строить |
| **D-06** | Признак отзывчивости грубым ведром; бейджа присутствия не делаем нигде | §5.5.3 | Мёртвая анкета подрывает доверие так же, как анкета без верификации | Отзывчивость копится позже и на новичке не появляется |
| **D-07** | Для публичной площадки в ленте показываем название места и район | §7.2 | Кафе и парк ничьей приватности не составляют, а решить «ехать или нет» прятать мешает | Точный адрес по-прежнему только после подтверждения |
| **D-08** | Обоснование «интент-чипы задают не-дейтинговый фрейм» снято | §6 | Тем же приёмом Tinder задаёт дейтинговый, а Meetup спрашивает намерение трижды | Чипы остаются как гигиена категории, отличием не считаются |
| **D-09** | Комиссия остаётся единственной целевой моделью; второй источник не выбираем | §9 | Meetup при 2–6 участниках зарабатывает подпиской участника — аргумент против подписки | ~~действует~~ → снято **D-28** |

## 2026-09-09 — разбор паттернов знакомства

| # | Решение | Где | Почему | Цена |
|---|---|---|---|---|
| **D-10** | **Swipe-first подтверждён:** колода — точка входа (вопрос В7) | §4 | Быстрее всех даёт первую ценность и знаком аудитории: 870 тыс. недельных пользователей дейтинга в Украине (**B**) | Разбор рекомендовал обратное. При низкой плотности колода упирается в пустое состояние раньше первой ценности — отсюда три обязательные компенсации в §4 |
| **D-11** | Прогрессивное расширение радиуса, когда колода исчерпана | §5.2 | Откладывает пустое состояние, не отменяя его | Расширяется только расстояние; фильтры не трогаются никогда |
| **D-12** | Метрика ликвидности считается по фиксированным 10 км | §11 | Иначе она растягивается вместе с радиусом и всегда зелёная | — |

## 2026-09-11 — после кабинетного ресерча о людях

| # | Решение | Где | Почему | Цена |
|---|---|---|---|---|
| **D-13** | Порядок чтения карточки: общие интересы первой строкой, расстояние последним | §5.2, §8 | Свайп в дружбе делается по совпадению, а не по внешности | Гипотеза H4 не проверена — порядок задан волевым решением |
| **D-14** | *Отменено 24.09 — `D-49`: ступеней нет.* Первая ступень Profile Strength **зарабатывается**, а не выдаётся | §5.5.2 | Ступень по факту регистрации — это бейдж отсутствия истории | Профиль без действий не показывает ничего |
| **D-15** | Повторяющаяся встреча входит в v1 | §5.4 | Холл (**A**): 40–60 часов до приятеля; одна встреча результата не даёт | Отдельная сущность в модели данных |
| **D-16** | Напоминание накануне `Still going?` и двухфазный счётчик | §5.4 | Бесплатное обязательство нарушить ничего не стоит; no-show на бесплатном кратно выше | Первое уведомление в продукте — появляется экран настроек и правило тона. *Уточнено 18.09: экрана настроек нет `D-38`, разрешение на пуш — `D-40`.* |
| **D-17** | Метрика повтора: пара с двумя и более подтверждёнными встречами за 30 дней | §11 | Все прежние метрики меряли вход, ни одна — повтор | Прокси: Холл меряет часы, мы меряем разы |
| **D-18** | Порог заполняемости check-in — 40 %, считается по участникам | §11 | По событиям порог вырождается: «хотя бы один ответил» набирается при ~10 % | Ориентир сверху — Airbnb 72–73 % при оплаченной сделке; 40 % это цель, а не данность |
| **D-19** | Роль соорганизатора — вне v1 | §10 | Тянет модель прав доступа и размывает роль организатора | → предмет исчез вместе с **D-29** |
| **D-20** | **Одиночество не продаём** (правило 13) | Рабочие соглашения | UNDP (**A**): в Украине об одиночестве сообщают 17 % молодёжи против 54 % уехавших — прямое обращение попадает в меньшинство | Называть **ситуацию** правило не запрещает: чипы `Just moved here` остаются |

## 2026-09-14 — персоны и jobs

| # | Решение | Где | Почему | Цена |
|---|---|---|---|---|
| **D-21** | Primary — Даша, переехала в Киев. Топ-3 jobs для MVP | §2.2 | Самый большой измеримый сегмент: 442 438 ВПО, ~12 % города (**A**), и единственный с исследованием ровно про него | **Не подтверждено**, что она ищет компанию через приложение: в измеренном канале переехавшие почти невидимы — 2 упоминания из 100 |

## 2026-09-15 — доисследование и замена единицы

| # | Решение | Где | Почему | Цена |
|---|---|---|---|---|
| **D-22** | **Событие с организатором заменено запросом на компанию. Посев отменён целиком** | §2.1, §5.4 | В Киеве 0,03 % действующих организаторов, медианная явка 2 (В-3). Лента, которую наполняет такая доля, не наполняется | Ставка на органику. Порог §11 в первые недели зелёным не будет, пустая лента запросов — главный экран старта |
| **D-23** | В запросе **занятие обязательно, дата опциональна** | §5.4 | Требование даты отсекало пять шестых спроса: запросов 18,3 %, с конкретным планом 5,6 % (В-4) | Без даты запрос сползает к анкете — держат три страховки: 14 дней жизни, дата при подтверждении, тест копирайта |
| **D-24** | `+1` — не дефолт, а **рекомендация**; интерфейс не подталкивает к большим числам | §5.4 | Приглашение в Киеве собирает один-два отклика (В-5); BlaBlaCar даёт тот же порядок | Замер сделан на старой единице — это ставка на порядок величины, а не факт |
| **D-25** | **Гендер разведён надвое:** фильтра в колоде нет; ограничить отклик можно **только своим полом** | §5.1, §5.2, §5.4, правило 6 | Фильтр подбора — дейтинг; ограничение приёма — безопасность (R2, Pew 57 %). Правило «только свой пол» убивает первое и сохраняет второе | В профиле появляется поле пола. Верификация пол не проверяет: защита от ленивого использования, не от лжи |
| **D-26** | У верификации появилось **состояние отказа**: причина, безлимитная пересдача, живой модератор после двух неудач, запрет блокировать за неудачу. *Уточнено 17.09: «не распознали» и «на ручной проверке» — два **состояния** экрана верификации, а не два экрана (§6, правило «экран или состояние»)* | §7.3 | В 250 отзывах 15 про верификацию, средняя 1,3, и **ни одной жалобы на ожидание** — все про ложный отказ (В-2) | Часть людей отказывается от биометрии принципиально: для них продукт закрыт, обходного пути не делаем |
| **D-27** | Селфи проверяет автоматика, человек — в запасе; цель по времени — минуты | §12 | Модель Bumble: смесь автоматики и живого просмотра, «notified within minutes» | Доля доходящих до конца верификации неизвестна — внешнего ориентира нет |
| **D-28** | **Целевая модель выручки потеряла предмет и открыта заново** | §9 | Замена единицы убрала роль организатора и платное мероприятие как явление — снимает **D-04** и **D-09** | До выбора модели правило 7 действует без изменений. Решать не раньше трёх месяцев работы |
| **D-29** | Роль организатора исчезла вместе с событием; соорганизатор потерял предмет | §10 | Запрос создаёт обычный участник и ни за что не отвечает — снимает **D-19** | Вернуть придётся, если запросы на `+N` вырастут в мероприятия |
| **D-30** | Новые метрики: **доля создающих** и доля запросов с ограничением по полу | §11 | Первая заменяет отслеживание посева, которого больше нет; вторая проверяет, не разошёлся ли продукт на два непересекающихся | Обе без внешнего ориентира — сравнивать не с чем |

## 2026-09-16 — инвентаризация сущностей

| # | Решение | Где | Почему | Цена |
|---|---|---|---|---|
| **D-31** | **Приватная обратная связь остаётся в MVP как гигиена безопасности**, а не как ответ на job. Job под неё не выдумываем | §7.5 | Ни один job из `jtbd.md` её не порождает. Она нужна **платформе** — ранний сигнал модерации до того, как ситуация станет инцидентом, — а не человеку напрямую. Тот же класс, что интент-чипы `D-08` | В интервью добавлен вопрос: «после встречи было не по себе — что сделали?». Если ответ есть — job появится и гигиена станет функцией; если нет — остаётся гигиеной |
| **D-33** | *Пересмотрено 23.09 — `D-43`: вкладок пять, профиль стал вкладкой. Разбор «почему не четыре» ниже сохранён как история.* **Три вкладки вместо четырёх:** `People · Plans · Chats`. Профиль и всё за ним — за аватаром в шапке, один тап отовсюду | §6 | Постоянная панель — дефицитное место, и оно для того, что делают **каждый день**. Job за профилем есть (S1, R2, G5), но действия редкие: профиль правят раз в неделю, верификацию проходят один раз. Четвёртая вкладка попала бы туда не по job, а по норме категории | Верификация теряет постоянную видимую точку входа — остаётся контекстный гейт `D-03`, и он важнее: человек идёт верифицироваться, когда хочет написать. Аватар менее очевиден новичку, чем подписанная вкладка — проверяется на прототипе. **Глубина main job не меняется: 3 тапа при любом варианте** |
| **D-34** | **Повтор предлагается после встречи, и начать его может любой участник**, не только автор | §5.4, §5.5.1 | Флаг «повторяется» стоял при создании — решение спрашивали **до того, как появилась информация для него**: кто придёт и как пройдёт. R4 при этом принадлежит Даше, а Даша обычно **участник**, не автор: право повтора только у автора закрывало бы самый доказанный job ядра для не той персоны. Ролей в продукте нет `D-29`, значит и права автора на следующую встречу нет по устройству | Поверхность для навязчивости: предложить повтор может тот, с кем не хотят повторять. Держат два ограничения — предложение доступно только участникам прошедшей встречи и живёт 14 дней; дальше работают Block и приватная обратная связь |
| **D-32** | **Переключатели приватности уходят из v1** — скрыть расстояние, домашний район вместо GPS, скрыть отзывчивость. **Инвариант остаётся:** точки на карте нет никогда, расстояние округлено | §7.1, §5.5.3, §6 | Job «контролировать, что обо мне видно» в списке нет, и данных под него нет. Защиту даёт **умолчание для всех**, а не настройка: округление и отсутствие точки — правило 5. «Домашний район вместо GPS» покрывается отказом от геолокации на онбординге | Человек, который хочет нулевого расстояния, в v1 может только отказаться от геолокации. Вернётся, если интервью покажут потребность — вопрос в гайд добавлен |

---

## 2026-09-17 — после потоков

| # | Решение | Где | Почему | Цена |
|---|---|---|---|---|
| **D-35** | **R3 остаётся групповым job.** Владельцы — Оля и Артём; Даши в нём нет, переформулировать не стали | `jtbd.md`, матрица | Job написан про группу — «встреча, где записалось восемь незнакомых людей». На `+1` восьми человек не бывает, и вопрос «сколько подтвердило» вырождается в «только я или никто». Расширять владение значило бы растянуть формулировку под механику, а не под ситуацию | **Названа и принята:** механизм R3 — напоминание и двухфазный счётчик — на паре работает, и работает сильнее, чем в группе: один неответ там означает, что встречи не будет вовсе. В матрице это не отражено. Чтобы прототип не недооценил счётчик на `+1`, действует `D-24`: пара проектируется первой независимо от того, чей это job |

| # | Решение | Где | Почему | Цена |
|---|---|---|---|---|
| **D-37** | **Основной путь к встрече идёт через чат, а не через запрос.** Колода → взаимный свайп → диалог → договорились о встрече. Запрос остаётся вторым путём и объектом, в который пара может вступить вместе | §1, §4, §5.3 | Так устроено решение PM о продукте: человек листает карточки, видит **чем увлекаются** другие, свайпает по совпадению интересов, и дальше разговор ведёт к встрече. Запрос при этом не отменяется — он закрывает тех, у кого уже есть план, и тех, кто не хочет ждать взаимности | **R1 переписан:** прежняя формулировка требовала «минуя разговор ни о чём», теперь разговор — дорога, а не помеха. **Путь стал вероятностным:** взаимность не в руках человека, и до первого действия 1 тап, а до встречи — неизвестно сколько. **Отличие «план как единица» перестало быть основным маршрутом** (§1): оно остаётся тем, чего нет у конкурентов, но primary до него доходит не всегда. **В §11 добавлена метрика** «доля диалогов, дошедших до назначенного дня» — без неё провал основного пути не виден |
| **D-36** | **Совместный отклик: пара, которая совпала, может вступить в чужой запрос вдвоём** | §5.4 | Прямое следствие `D-37`: если двое договорились в чате, им нужен способ пойти куда-то вместе, не изобретая свой план с нуля | **Работает только на `+2` и больше**, а рекомендованный размер — `+1` (`D-24`). Пара в `+1` не помещается по устройству. Если совместный отклик окажется частым, рекомендация `+1` начнёт его душить — следить по метрике §11 |

## 2026-09-17 — после трассировки

| # | Решение | Где | Почему | Цена |
|---|---|---|---|---|
| **D-38** | **Пять экранов — состояния, а не экраны; «Настройки» уходят в backlog.** Splash → загрузка при старте экрана входа; возрастной гейт → поле «дата рождения» и отказ «младше 18» в настройке профиля; геолокация вкл/выкл → состояние экрана разрешения; приватная обратная связь → необязательное поле на «Кто дошёл». Экран «Настройки» (аккаунт, уведомления) не рисуется | §6, §7.5 | Матрица покрытия jobs × экраны (`sitemap.md`, «Трассировка») показала восемь экранов без job. По правилу §6 «экран определяется объектом» четыре из них оказались статусами других объектов, а «Настройки» — контейнером, всё содержимое которого либо уже имеет свой экран (заблокированные, геолокация, профиль), либо не требует ни один job. Правила при этом не меняются: 18+ остаётся, `D-31` остаётся, `D-32` остаётся | **Онбординг стал на два экрана короче, но не на два шага:** дату рождения всё равно вводят, загрузку всё равно ждут. **Удаление аккаунта** — требование закона, и места под него теперь нет: вернётся как состояние экрана входа, когда дойдёт до продакшена. **Обратная связь и check-in на одном экране:** поле «Anything off?» видно рядом с «кого видел», и часть людей прочтёт его как оценку человека — копирайт обязан развести их (§7.5) |

## 2026-09-18 — после разбора тупиков и состояний

| # | Решение | Где | Почему | Цена |
|---|---|---|---|---|
| **D-39** | **Назначенный в чате день — это запрос `+1` с датой.** В диалоге 1:1 любой из двоих предлагает день: занятие (обязательно, `D-23`) + день, место опционально. Второй принимает у себя одним тапом; принятие создаёт запрос на двоих с датой сразу в статусе «встреча назначена». Такой запрос в ленте не показывается — это записано здесь как правило, а не выведено из статуса; живёт в «Моих планах» и в шапке диалога. Дальше как у любой встречи: напоминание, check-in, повтор | §4, §5.3, §5.4, §11 | По карте связей `sitemap.md` встречу порождает только запрос, а у чата 1:1 продолжения в встречу нет: основной путь `D-37` доходил до «договорились», и на этом объект кончался — нечего напоминать, засчитывать и повторять, R3, R4, S1 и метрика §11 на основном пути не работали. Новой сущности не нужно: у запроса уже есть занятие, дата, состав и статус «встреча назначена» | Пара со взаимным лайком оказывается в `+1` как автор + один — это **не** совместный отклик `D-36` (там пара — гости), и копирайт с «Моими планами» обязаны это различать. Согласие второго явное — «втащить нельзя» действует. Ролей не появляется `D-29`: предложить день может любой. Правило «собранные из чата не в ленте» — первое правило видимости запроса; если понадобится второе, это уже флаг приватности, и его придётся решать отдельно. Метрика «доля диалогов, дошедших до дня» считается по факту такого запроса |
| **D-40** | **Разрешение на уведомления — состояние карточки запроса, не экран и не настройка.** Запрашивается при первом подтверждённом плане (in-app sheet, затем системный диалог); отказ — состояние карточки «уведомления выключены: напоминание и check-in здесь», и вход к напоминанию, check-in и предложению повторить — из «Моих планов» | §5.4 «Напоминание и счётчик», §6 | Напоминание `D-16`, check-in и повтор `D-34` приходят пушем, а разрешения нигде не было. `D-38` отправил «уведомления» в backlog как настройку; разрешение — не настройка, а условие механизма. Момент — тот же принцип, что гейт `D-03`: спрашивать, когда есть объект и мотивация | Геолокация — свой экран VII.7, а уведомления — состояние: у геолокации есть свой объект (радиус и расстояние на карточках), у разрешения на пуш объекта нет — он служит карточке плана, поэтому по правилу §6 живёт на ней. Тонкой настройки не появляется — цена `D-16` не оплачивается. На мобильном вебе (§3) пуш ненадёжен: iOS даёт его только установленному PWA — у каждого пуша обязан быть in-app-дублёр, и без пуша путь к напоминанию и повтору 3–4 тапа. Экранов не прибавляется: 30 |
| **D-41** | **Автор может отменить свой запрос** — то же действие, что `Can't make it`, с карточки в любой момент. На `+1` это «встреча отменена» с пушем второму; на `+N` запрос живёт без него — автор не ведёт группу | §5.4 | У автора не было способа выйти из своего плана; вечером перед `1 confirmed` он не мог ничего сделать (flows R3) | Новый статус запроса «отменён»; на `+1` отмена одним = отмена для обоих, и это не роль (`D-29`), а свойство пары. Состояния: III.3 «отменён» / «отменён автором», V.1 «встреча отменена» |
| **D-42** | **Ожидание чужого решения — отдельный класс состояний, а не загрузка.** Пять мест: отклик до ответа автора, совместный отклик до согласия второго и до подтверждения автора, селфи на ручной проверке, `going` до `confirmed`. Такое состояние не рисуется спиннером и обязано называть, кто должен ответить, сколько оно живёт и что можно сделать вместо ожидания | §6, дерево экранов `sitemap.md` | Продукт вероятностный по устройству (§4, `D-37`): половина его состояний — про чужое решение, а не про сеть. Если не назвать класс, на вайрфреймах вместо статуса появится спиннер, и человек прочитает чужое молчание как сбой приложения. Разбор состояний главного потока (`wireframes/_screens.md`) нашёл эти пять мест в одном наборе из восьми экранов | Раскладок становится больше: каждое ожидание рисуется отдельно от загрузки, и у каждого свой копирайт. Сроки, которые состояние обязано называть, есть не везде: у отклика он равен сроку запроса (§5.4), у ручной проверки цель «минуты» `D-27` — но это цель, а не факт `[?]` |
| **D-43** | **Пять вкладок вместо трёх: `People · Events · Search · Chats · Profile`.** Профиль становится вкладкой; появляется **Поиск** — одно поле, результаты двумя группами: люди и планы | §6, дерево экранов `sitemap.md` | Решение PM о продукте. Отменяет `D-33` в части «профиль не вкладка»: довод «профиль правят раз в неделю» остаётся верным, но перевешивает то, что человек ищет вход к себе там, где привык, — в нижней панели. Поиск закрывает то, чего не делают ни колода, ни лента: **найти конкретное** — человека по имени или интересу, план по занятию; ближе всего к нему job G3 «компания под конкретное хобби» | Панель из пяти пунктов на 390px: подписи становятся короче, иконки обязательны — заполнители в вайрфрейме это уже показали. **Поиск — новая сущность и новый экран `IX.1`**, а значит ещё четыре страницы вайрфрейма и строка в матрице покрытия. Глубокий слой худеет: за аватаром больше нет входа, всё за ним переезжает во вкладку. Главное: поиск требует **того, что нечего искать при пустой базе** (§2.1) — пустой результат станет частым экраном наравне с пустой колодой |
| **D-44** | **`Report` и `Block` доступны в один тап оттуда, где виден человек, — но не обязательно на самом экране.** С карточки в колоде кнопка убрана: жалоба и блокировка живут в профиле человека (тап по карточке) и в чате | §7.4, правило 4 Рабочих соглашений | Карточка в колоде — экран одного решения: общие интересы, имя, сигналы доверия, расстояние и два действия. Кнопка `More` рядом со `Skip` и `Like` конкурирует с ними за внимание там, где человек ещё ничего плохого не увидел. Профиль открывается тапом по карточке (§5.2) — это один тап | **Правило 4 ослаблено, и это цена.** Человеку, которому стало неприятно **на колоде**, теперь нужен лишний тап, и делает он его в неудачный момент. Если по метрике §11 доля жалоб с колоды окажется заметной или интервью покажут, что люди не находят, куда пожаловаться, — кнопка возвращается на карточку |
| **D-45** | **В интерфейсе единица называется `event`, а не `plan`.** Меняется только английский копирайт: вкладка `Events`, `Events nearby`, `Post an event of your own`, `Look at events`. Внутренний термин «запрос на компанию» и устройство единицы не меняются | Правила 6 и 13 Рабочих соглашений, все 44 вайрфрейма | Решение PM о копирайте. `Plan` в разговорной речи — это «намерение», и на карточке рядом с датой, местом и счётчиком мест он читался как черновик, а не как то, куда можно прийти | **Слово возвращает в копирайт мероприятие, которого в продукте нет.** `Event` обещает афишу, организатора и билет — ровно то, что убрали `D-22` и `D-29`: автор события ни за что не отвечает, роли организатора нет, карточки мероприятия нет. Если на тестах люди начнут спрашивать «кто ведёт» и «сколько стоит», слово придётся менять обратно или признавать, что вернулась роль. Второе следствие: §9 держит комиссию на случай, если события на `+N` вырастут в платные мероприятия, — теперь эта фраза читается как тавтология, и порог «выросли» надо будет определять не словом, а метрикой |
| **D-46** | **Создание события живёт на вкладке Events, а не в каждом пустом состоянии.** Из пустой колоды остаётся один выход — `Look at events`; кнопка `+` встречает в шапке ленты. Там же в пустой колоде «расширить поиск» и «снять фильтры» встали одной строкой | §2.1 п. 3, §6, макет пустой колоды | Решение PM. Кнопка «создать своё» в пустой колоде вела в никуда: экрана III.4 в наборе нет, а два вертикальных действия подряд спорили с главным — выходом во второй путь. Создание не исчезает: оно в одном тапе, на вкладке, где живут события | **Обязательство §2.1 п. 3 ослаблено, и это цена.** Путь «создать своё» из пустой колоды стал на тап длиннее ровно там, где плотность ниже всего, — а именно на старте (§2.1, `D-22`). Если доля создающих (§11) останется у 1 %, кнопку придётся вернуть в пустую колоду или признать, что низкий порог создания держится не на ней |
| **D-47** | **Радар «как это работает» — последний экран онбординга, а не старт сессии.** Кольца показывают близость, кружки — что вокруг есть люди и события, две кнопки `People` и `Events` объясняют два пути и задают, какая вкладка откроется первой. **Данные иллюстративные**, живых людей экран не показывает и говорит об этом строкой на себе | §4, §6, дерево экранов `sitemap.md` (VII.8, ветка «Вход в продукт»), задание `wireframes/_screens.md` | Решение PM, уточнённое разбором. Экран отвечает на вопрос, который человек задаёт раньше остальных, — «как это вообще устроено» — и честно показывает оба контура сразу, а не прячет второй за выходом из первого. Первая версия решения ставила радар стартом каждой сессии на живых данных; от неё отказались до того, как она попала в макеты набора | **Что цена перестала быть.** На живых данных радар был витриной плотности в продукте, чей главный риск — плотность (§2.1): при пустой базе он первым делом показывал, что вокруг никого, и делал это каждую сессию. Иллюстративные данные это снимают, а заодно снимают риск §7.1 — показывать нечего. Глубина вернулась к 3 тапам, `D-10` не пересмотрен: сессия открывается колодой. **Что цена есть.** Ещё один шаг в онбординге ради того, что человек через минуту увидит сам. Если конверсия онбординга (§11) просядет, экран убирается первым. И вторая: иллюстрация обещает плотность, которой может не быть, — строка «This is an example» защищает от обмана, но разочарование на следующем экране не отменяет |
| **D-48** | *Заменено 24.09 — `D-49`: ступеней нет нигде.* **Ступень Profile Strength не показывается на карточке колоды.** Там остаются `Verified`, счётчик подтверждённых встреч и признак отзывчивости; ступень видна в профиле человека — один тап — и в своём профиле | §5.2 (порядок чтения `D-13`), §5.5.2, правило 12 | Решение PM. На карточке ступень пересказывает то, что рядом и так видно: фотографии, интересы, `Verified` — то есть занимает место четвёртой плашки, ничего не добавляя к решению. Вдобавок слово `Regular` неоднозначно и требует легенды, а бейдж, который надо объяснять, за секунду взгляда не работает | **На пустой базе ступень была единственным сигналом, который работает без других людей** (§5.5.2): счётчик встреч у всех нулевой, отзывчивости ещё нет, а ступень человек зарабатывает сам в первый день. Убрав её с карточки, мы ослабили доверие ровно там, где плотность ниже всего. Если на тестах окажется, что по карточке нечем отличить заполненный профиль от пустого, ступень возвращается — или на карточку, или в виде другого признака полноты |
| **D-49** | **Ступеней профиля нет.** Механизм Profile Strength (`Newcomer → Regular → Local`) убран целиком: ни на карточке, ни в профиле человека, ни в своём. Вместе с ним уходят экран I.4 «Что поднимет уровень» и метрика §11 «доля профилей выше Newcomer». Репутация остаётся на двух механизмах: подтверждённые встречи и признак отзывчивости | §5.5, §5.5.2, §11, §12, правила 11 и 12, дерево экранов `sitemap.md`, поток S1 | Решение PM. Ступень пересказывала то, что видно рядом: фотографии, интересы, `Verified`. Слово требовало легенды — бейдж, который надо объяснять, за секунду взгляда не работает. **Отменяет `D-14` и `D-48`** | **Ступень была единственным сигналом доверия, работающим при пустой базе** (§2.1): счётчик встреч у всех нулевой, отзывчивости ещё нет, а ступень зарабатывалась в первый день. В первые недели на карточке останется один `Verified`, и заполненный профиль внешне не отличается от пустого — ровно там, где плотность ниже всего. Второе: job S1 «выглядеть тем, кому можно доверять» теряет свой главный инструмент и держится теперь на верификации и встречах. Если тесты покажут, что решать по карточке не по чему, вернётся не ступень, а другой признак полноты профиля |
| **D-50** | **Профиль — хаб со ссылками, а списки живут там, где с ними работают.** Свои события — срез `Mine` вкладки Events (экран III.6), матчи без переписки — полоса «New matches» вверху списка чатов. В профиле они перечислены строками `My events` и `My matches`, туда же добавлены `Settings` и `Log out` | §6, дерево экранов `sitemap.md` (III.6), макеты my-events, chats, my-profile | Решение PM после разбора. Матч живёт часами, а профиль открывают раз в неделю (довод `D-33`, `D-43`): спрятать матчи в профиль значит удлинить самый хрупкий путь — до первого сообщения (TTFC, §11). То же с откликами: подтверждение автора держит второй контур, и путь к нему не должен расти. Шестую вкладку не заводим: на 390px это 65px на пункт вместо 78 | **Профиль стал длиннее и разнороднее:** в нём и «как меня видят», и ссылки на чужую работу, и аккаунт. Если по тестам окажется, что строки-ссылки не нажимают, они лишние — и тогда профиль возвращается к одному делу: как меня видят. Вторая цена: `D-38` ослаблен наполовину — экрана настроек по-прежнему нет, но строка `Settings` в профиле есть, и за ней придётся что-то показать |
| **D-51** | **У чужого профиля нет состояния ошибки.** Догрузка профиля показывается одной загрузкой: фото, дошедшее не целиком, стоит **заблюренным** и становится чётким по мере прихода, на месте ещё не пришедших событий — скелет. Страница `profile-error` удалена из набора | §5.2 брифа, дерево `sitemap.md` I.1, `_screens.md`, `_conventions.md` | Решение PM. Человек открыл профиль, чтобы решить — свайпать или нет, — и для этого у него уже всё есть: интересы, сигналы доверия и оба действия. Полоса «связь оборвалась» занимала место решения и сообщала то, чем он не распорядится. Размытое фото говорит ровно то же самое — «ещё не дошло» — и не уводит взгляд | **Человек не узнаёт, что связь оборвалась**: размытая фотография читается как «грузится», даже если данные не придут совсем. Терпимо ровно потому, что от этой части не зависит ни одно действие экрана. Там, где зависит — лента, список чатов, отправка отклика, — ошибка по-прежнему называется вслух и даёт `Try again`. Если тесты покажут, что люди подолгу ждут у размытого фото, вернётся не экран ошибки, а тихая строка под фотографией |
| **D-52** | **Не отправленное сообщение помечается само, отдельного блока об ошибке в диалоге нет.** Пузырь несёт подпись `Not sent · tap to try again` и иконку повтора в правом углу; нажатие на пузырь повторяет отправку. В UI к этому добавится красный оттенок (`--danger`, §8) | §5.3 брифа, макет `dialog-error`, `_conventions.md` | Решение PM. Блок «Message didn&rsquo;t send» с кнопкой `Try again` стоял отдельно от реплики: сообщал об аварии в стороне от того, что сломалось, занимал место разговора и повторял подпись, которая и так была под пузырём. Сбой одной реплики — состояние реплики, а не экрана | **Заметность падает.** Полоса с заголовком бросалась в глаза, подпись под пузырём — нет: человек может пролистать вниз и не увидеть, что сообщение не ушло. В сером вайрфрейме это держат иконка и подпись, в UI — цвет; если тесты покажут, что люди всё равно не замечают, вернётся не блок, а неотключаемая полоса в шапке диалога |
| **D-53** | **Выходы из чата не исчезают после назначенного дня.** `Suggest a day` и `Invite together` остаются в диалоге и в состоянии «день назначен» | §5.3 брифа, макет `dialog-success` | Решение PM. Раньше кнопки убирались с доводом «день назначен, предлагать нечего». Но встреча одна, а разговор продолжается: договориться о следующей субботе, позвать в чужое событие вдвоём (`D-36`) или назначить второй день — всё это нормальные продолжения. Продукт обещает **повтор** (R4, §5.4) и не может закрывать дорогу к нему в единственном месте, где живут уже найденные люди | **Экран стал плотнее:** под карточкой события идут строка о напоминании, выход в «Мои события» и ещё два действия — четыре точки внимания там, где раньше было две. Если на тестах окажется, что после назначенного дня люди путаются, что нажимать, кнопки уедут под карточку события свёрнутой строкой, но не исчезнут |
| **D-54** | **Действие живёт на превью события, а не только на карточке.** В строке ленты, в результатах поиска и в событиях чужого профиля стоит `Ask to join`; там, где мест два и больше, рядом — `Invite together`. В списке «Позвать вдвоём» строка несёт `Ask together`. Заголовок строки по-прежнему открывает карточку | §5.4 брифа (лента и отклик), макеты `feed`, `search`, `profile`, `invite`, `_conventions.md` | Решение PM. Открывать событие ради кнопки, которую можно поставить в строке, — лишний тап на детерминированном пути (§6: глубина до main job — 3 тапа, и один из них был этим). Карточка остаётся для решения «идти или нет»: место, описание, кто автор | **Отклик стало легче отправить не глядя.** Из строки не видно ни места встречи, ни описания, ни ограничения по полу целиком — человек откликается по трём строкам. Для автора это шум: подтверждать придётся тех, кто не читал. Гейт верификации (§7.3) и отзыв отклика в любой момент (§5.4) остаются, но если доля отозванных откликов вырастет, кнопка из строки уйдёт обратно в карточку. Второе: строка выросла на 44px — в ленте помещается меньше событий |
| **D-55** | **Превью события: фото слева, заголовок с датой справа, описание под ним, действие снизу.** Фото — **автора**, а не обложка события: поля «обложка» в единице (§5.4) нет, а фотография у человека обязательна (§5.1). Кружок аватара из строки автора убран — он повторял то же фото | §5.4 брифа, макеты `feed`, `search`, `profile`, `invite`, `my-events`, `_conventions.md` | Решение PM по эскизу. Строка из трёх текстовых линий не отличалась от строки чата и не давала опоры взгляду; фото — первое, что человек считывает, и оно же несёт «кто зовёт» — сигнал доверия до первого сообщения (правило 12) | **Событие в ленте теперь читается через человека.** На `+1` это честно — идти вдвоём с ним и есть весь план; на `+N` фото одного автора рядом с занятием, куда идут пятеро, обещает больше, чем есть: роли организатора нет (`D-29`). Второе: **обложки события в продукте так и нет** — если она понадобится, это новое поле единицы, шаг в сторону афиши, от которой ушли `D-22`. Решать отдельным решением, а не вёрсткой |
| **D-56** | **У события есть обложка — минимум одна фотография, обязательная при создании.** Без неё событие не публикуется. Обложка идёт первой в превью (квадрат слева) и на карточке (во всю ширину, полос пагинации столько же, сколько фото). Фотография автора туда не годится: снимок описывает **занятие**, поэтому у автора остаётся свой аватар в строке с именем. **Отменяет часть `D-55`**, где превью показывало фото автора | §5.4 (поля единицы, создание), §12 (мок-данные), макеты `feed`, `search`, `profile`, `invite`, `my-events`, `plan`, `_conventions.md` | Решение PM. Строка события без картинки не отличалась от строки чата, а фото автора обещало человека там, где речь о занятии. Картинка — то, что человек считывает первым, и она же единственный способ показать, **что** будет происходить, до открытия карточки | **Порог создания вырос, и это бьёт по самой хрупкой метрике.** `D-22` строит второй контур на низком пороге: создать событие — один шаг, доступный в первый день; теперь в этот шаг входит поиск и загрузка фотографии, а «доля создающих ≥ 1 %» (§11) — метрика, которая ломается первой. Смягчения, которые мы **не** взяли: стоковые картинки к шаблонам (лента станет одинаковой) и автоподбор по тегу (продукт врёт о том, чего не было). Если доля создающих просядет, первым отменяется обязательность, а не сама обложка. Второе: фотография — шаг в сторону афиши, от которой ушли `D-22` и `D-29`; роль организатора при этом не возвращается. Третье: новая поверхность для модерации — жалоба `inappropriate photos` (§7.4) теперь относится и к событиям |
| **D-57** | **В превью события от автора остаётся один признак — `Verified`, под датой.** Фото и имя автора из строки убраны: превью про занятие. Автор целиком — аватар, имя, счётчик встреч, отзывчивость — остаётся на карточке события (III.3). В «Моих событиях» строка не меняется: там свои события и те, куда уже идёшь | §5.4 брифа, правило 12, макеты `feed`, `search`, `profile`, `invite`, `_conventions.md` | Решение PM. После обложки `D-56` в строке сталкивались две картинки — снимок занятия и аватар автора, — и человек читал строку про человека, а не про дело. `Verified` под датой отвечает на единственный вопрос, который решается до открытия карточки: можно ли вообще иметь дело с этим событием | **Из ленты ушли два сигнала доверия из трёх.** Счётчик подтверждённых встреч и признак отзывчивости больше не видны до открытия карточки, а job R2 «риск понятен до выхода из дома» на ленте держится теперь на одной верификации. Правило 12 при этом цело: оно требует сигналы **до первого сообщения**, а сообщение отсюда не отправляется — только отклик, после которого автор всё равно подтверждает. Если на тестах окажется, что люди откликаются вслепую и жалеют, в строку вернётся счётчик встреч — цифрой рядом с `Verified`, без имени и фото |
| **D-58** | **Превью события читается тремя уровнями.** Первая строка — **что и когда**: заголовок во всю ширину, дата в конце строки, мельче и тише. Вторая — **где и далеко ли**: площадка, округлённое расстояние, цена. Третья — **сколько мест и `Verified`**. **Высота карточки фиксирована** (176px с действием, 116px без), заголовок — максимум две строки. Обложка 104px слева **во всю высоту строки** — под ней не остаётся пустоты, и карточка читается двумя блоками: картинка и текст с действием. Обложка кончается над строкой действий, а действие тянется **во всю ширину карточки**; когда действий два, вторичное сокращается до `Invite` — полное `Invite together` остаётся на карточке события | §5.4 брифа, `_conventions.md`, макеты `feed`, `search`, `profile`, `invite`, `my-events` | Решение PM после разбора: строка не читалась. Дата отдельным столбцом отнимала у заголовка треть ширины, и «Morning run along the embankment» рвался на три строки; пять фактов через `·` шли одной кашей; `Verified` стоял под датой, хотя относится к людям. Теперь у каждой строки одна работа, и глаз идёт сверху вниз по убыванию важности | **Фактов в превью стало меньше.** Район («Podil» у публичной площадки) и теги ушли на карточку события: в три строки помещается площадка, расстояние и цена, но не всё сразу. Вторая цена — высота: 145px на строку вместо 170, но не 110, как в эскизе: обложка 72 и тач-цель кнопки 44 (§8) сжаться не могут. В экран помещается три события вместо двух |
| **D-59** | **Поле поиска стоит наверху, рядом с ним — фильтры.** Заголовок экрана — короткий `Search`, под ним строка: поле во всю ширину и кнопка `Filters`. Внизу поля больше нет | Макеты `search` и три его состояния, §6 брифа (список экранов), `_conventions.md` | Решение PM. Внизу поле стояло по образцу чата, но там его держит переписка: ввод продолжает разговор и должен быть под большим пальцем. В поиске человек **начинает** с запроса, и поле обязано быть там, где начинается экран; фильтры сужают ту же выдачу, поэтому стоят в той же строке — как `Filters` в колоде и ленте | **Поле уехало из зоны большого пальца.** На 390×844 верх экрана — самое неудобное место для набора, и уточнять запрос теперь дальше тянуться. Это цена за то, что поле видно сразу, а не после прокрутки выдачи. Второе: строка запроса съела высоту у результатов — в первый экран помещается на одну карточку меньше |

## Что открыто

| Вопрос | Где | Когда решаем |
|---|---|---|
| Целевая модель выручки | §9 | После трёх месяцев работы — когда станет видно, вырастают ли запросы в мероприятия |
| Доля доходящих до конца верификации | §11 | Только на своих данных: эталоны не публикуют |
| Кто ведёт посев — **снят** | — | Предмета нет с **D-22** |
| Настройки приватности | §7.1 | Если интервью покажут потребность скрывать расстояние совсем — **D-32** пересматривается |
| Порог «мало данных» для среза плотности на интенте | §2.1 п. 4, sitemap VII.6 | На данных прототипа: ниже порога число не показывается |

Полные разборы, счёт и оговорки — в [`research/research.md`](./research/research.md).
