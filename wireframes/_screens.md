# Ключевые экраны главного потока — задание на вайрфреймы

> Отобрано из [`../sitemap.md`](../sitemap.md) (дерево экранов, состояния),
> [`../flows.md`](../flows.md) (поток M1 и R1) и [`../research/jtbd.md`](../research/jtbd.md)
> (формулировки job). Названия экранов — **дословно из дерева**, вместе с кодами.
>
> Остальные потоки — R2, R3, R4, R5/S2, S1 — в этом файле не разбираются.

**Кто идёт по этому пути.** Даша, primary `D-21`: ищет, с кем провести свободное время.
Сторона автора (тот, кто зовёт) — другой человек и другой поток.

**Правило отбора.** Экран входит, если выполнены три условия:

1. он лежит на пути **ищущего** в потоке M1 — от открытия приложения до назначенного дня;
2. у него есть job в дереве `sitemap.md` — не «как-то касается», а тег экрана;
3. он есть узлом в диаграмме `flows.md`.

Восемь экранов из тридцати. Что не вошло и почему — в конце.

---

## 1. Экраны, задачи, место в потоке

| # | Экран (как в `sitemap.md`) | Какую задачу решает — строка из `jtbd.md` | Место в потоке `flows.md` |
|---|---|---|---|
| **II.1** | **Колода** | **M1:** «Когда мне есть куда пойти, но не с кем… я хочу, чтобы у моих планов была компания, а у компании — продолжение» | M1, вход: `Start → Deck`. Первый экран после онбординга, начало основного пути `D-37` |
| **I.1** | **Профиль другого человека** | **R2:** «…а всё, что я о нём знаю, он рассказал о себе сам, — я хочу понимать, чем рискую, до того как соглашусь на место и время» · **G1:** «за несколько секунд понять, стоит ли с ним встречаться» | M1: `HasPeople →\|да\| Person → Trust{Интересы совпадают и профиль внушает доверие?}` |
| **II.3** | **Совпадение — модальный момент, «Say hi»** | **M1** (второй человек найден) · **E2:** «Когда я делаю первый шаг к незнакомому человеку, я хочу не чувствовать себя навязчивой, чтобы этот шаг вообще сделать» | M1: `Mutual →\|да\| Match`. Единственная точка, где путь становится двусторонним |
| **I.5** | **Верификация — селфи по позе** | **R2** (тот же риск, но с моей стороны) · **G5:** «Когда продукт меня проверяет, я хочу, чтобы у меня был способ доказать, что я — это я, чтобы не остаться снаружи из-за чужой ошибки» | M1: `Match → IsVerified →\|нет\| Gate → Now → Verify`. Гейт перед первым сообщением `D-03` |
| **IV.2** | **Диалог** | **R1:** «Когда мы совпали и переписка идёт, а до встречи не доходит… я хочу, чтобы у разговора был выход в конкретный день, чтобы мы увиделись, пока интерес живой» · **M1** | M1 и R1, центр: `Dialog → Exit{Как выходим из разговора?}`. Основная дорога к встрече `D-37` |
| **IV.3** | **Позвать вдвоём в запрос** | **R1** (второй из трёх выходов из разговора) · **M1** · **E2** (вдвоём порог ниже) | M1 и R1: `Exit →\|зовём вдвоём в чужой запрос\| Pair → SecondAgrees → AuthorPair` `D-36` |
| **III.1** | **Лента запросов** | **M1** (второй путь к той же компании) · **G2:** «хочу куда-то пойти, а компании нет» · **G3:** «занятие есть, компании нет» | M1, ветка «ВТОРОЙ ПУТЬ»: `Feed → HasPlans`. Не зависит от взаимности, поэтому не убран в глубину (§4) |
| **III.3** | **Карточка запроса** | **M1** · **R1** (то, во что разговор выходит) · **R2** (что за место и кто автор) | M1: точка схода обоих путей — `PlanCard → Ask → Wait → AuthorReplied` (второй путь) и `DayOK →\|да\| FromChat` (день из диалога `D-39`) |

---

## 2. Состояния

`✓` — состояние реально: оно описано в дереве `sitemap.md` или есть узлом в потоке.
`—` — сценарий его не предусматривает. **Успех — только там, где есть отдельное
состояние «получилось»**, а не «действие прошло без ошибки».

| Экран | Пустой | Ошибка | Загрузка | Успех |
|---|---|---|---|---|
| **II.1** Колода | ✓ | ✓ | ✓ | — |
| **I.1** Профиль другого человека | ✓ | ✓ | ✓ | — |
| **II.3** Совпадение | — | — | — | ✓ |
| **I.5** Верификация | — | ✓ | ✓ | ✓ |
| **IV.2** Диалог | ✓ | ✓ | ✓ | ✓ |
| **IV.3** Позвать вдвоём в запрос | ✓ | ✓ | — | ✓ |
| **III.1** Лента запросов | ✓ | ✓ | ✓ | — |
| **III.3** Карточка запроса | — | ✓ | ✓ | ✓ |

**Отмечено 23 состояния на восьми экранах** — плюс заполненный вид каждого экрана,
отдельная раскладка «разговор без даты» у диалога и четыре ожидания из таблицы ниже.
У «Совпадения» и у успеха верификации отдельного заполненного вида нет: там состояние
и есть экран.

### Пятое состояние: ожидание чужого решения `D-42`

Четыре из восьми экранов заставляют ждать не данные, а другого человека — днями.
Это не загрузка: спиннер сказал бы, что тормозит приложение, а тормозит чужое молчание.
По `D-42` такое состояние называет **кто должен ответить, сколько это живёт и что можно
сделать вместо ожидания**.

| Экран | Чего ждём | Сколько живёт |
|---|---|---|
| **I.5** Верификация | живого модератора после двух неудач | цель — минуты `D-27`, но это цель, а не факт `[?]` |
| **IV.2** Диалог | что второй примет предложенный день `D-39` | `[?]` — срока нет |
| **IV.3** Позвать вдвоём | согласия второго, потом подтверждения автора `D-36` | `[?]` — срока нет |
| **III.3** Карточка запроса | что автор ответит на отклик | пока жив запрос: 14 дней без даты или до даты (§5.4) |

Два из четырёх сроков не заданы — это входит в задание на вайрфреймы: **либо копирайт
называет срок, либо мы его назначаем**.

### Почему так — по каждому экрану

**II.1 Колода.**
Пустой — `ПУСТАЯ (проектируется первой)` и `пусто по фильтрам — Reset filters`: на старте
это **первый реальный экран** (§2.1, `D-22`), а не edge case.
Ошибка и загрузка — узлы `DeckErr` и `DeckLoad` в потоке.
Успеха нет: лайк не даёт экрана «получилось» — успех колоды вынесен в отдельный экран
«Совпадение». Состояние `лайк отправлен` — подтверждение действия, не результат.

**I.1 Профиль другого человека.**
Все три состояния — **частичные**: шапка (имя, общие интересы, сигналы доверия) приходит
вместе с карточкой колоды и рисуется сразу, догружаются остальные фото и ближайшие
запросы человека (§5.2).
Загрузка — скелет галереи и списка планов при готовой шапке.
Ошибка — дозагрузка не дошла: экран остаётся рабочим, повтор только для непришедшей части.
Решение «свайпать или нет» должно оставаться возможным при любом из трёх состояний.
Пустой — у человека нет активных запросов: при плотности §2.1 это **частый вид, а не край**.
Успеха нет: решение отсюда ведёт обратно в колоду.

**II.3 Совпадение.**
Только успех — и это **эталон «отдельного экрана „получилось“»**: единственная его задача —
сообщить, что взаимность случилась, и открыть дорогу к `Say hi`. Пустого и ошибки
не бывает: экран существует, только когда результат уже есть. Загрузки нет — оба профиля
на руках, это модальный момент поверх колоды.

**I.5 Верификация.**
Ошибка — три разных отказа: `не распознали — причина, пересдать`,
`камера недоступна / доступ к камере отклонён`, `результат ручной проверки: отказ с причиной`.
Загрузка — `обработка`: секунды, пока автоматика смотрит селфи.
Ожидание — `на ручной проверке` (`D-42`): ждём живого модератора, и это уже не загрузка.
Успех — `пройдена`: после него открывается поле ввода в чате, поэтому это «получилось»
с последствием, а не галочка.
Пустого нет: экран-действие, показывать нечего.

**IV.2 Диалог.**
Пустой — `пустой (ice-breaker)`: подсказки существуют ровно ради этого состояния.
Ошибка — `сообщение не отправилось`.
Успех — `предложение дня: принято — запрос создан` (`D-39`): момент, когда разговор
превратился в день.
Загрузка — история сообщений при входе из списка чатов и из подтверждённого отклика;
из совпадения чат открывается сразу пустым, грузить нечего.
Ожидание — `предложение дня: ждёт ответа второго` (`D-42`).
**Отдельно:** `РАЗГОВОР БЕЗ ДАТЫ` — не пустота и не ошибка, а **провал R1**, главный
провал продукта. Рисуется как самостоятельная раскладка сверх четырёх состояний.

**IV.3 Позвать вдвоём в запрос.**
Пустой — следует из `D-36` и `D-24`: запросы на `+1` в выборе скрыты, а `+1` —
рекомендованный размер, значит выбирать может оказаться не из чего.
Ошибка — три отказа на одном экране: `второй не верифицирован` (условие не выполнено),
отказ второго, `отклонено` автором.
Успех — `подтверждено`: пара принята целиком, открывается чат запроса.
Загрузки нет: список запросов для выбора приходит из уже открытой ленты.
Ожидание — `ждёт согласия второго` и `ждёт автора` (`D-42`): две подряд, и обе на дни.

**III.1 Лента запросов.**
Пустой — `ПУСТАЯ (главный экран старта, D-22)` наравне с пустой колодой, и
`пусто по фильтрам`. Ошибка и загрузка — узлы `FeedErr` и `FeedLoad`.
Успеха нет: лента — витрина, результат даёт карточка.

**III.3 Карточка запроса.**
Ошибка — `отклик не отправился` (`AskErr`), `ОТКЛИК ОТКЛОНЁН`, `отменена автором` (`D-41`).
Успех — `подтверждена` и `+1 с датой из диалога` (`D-39`): здесь видно, что день назначен
и чат открыт.
Пустого нет: занятие — обязательное поле (`D-23`), карточка без содержимого не существует.
Загрузка — только при холодном открытии: из пуша или по ссылке; из ленты данные уже
на руках. Правило то же, что у профиля.
Ожидание — `жду ответа автора` (`D-42`), и это не загрузка.

---

## 3. Что не вошло и почему

| Экран | Почему не в наборе |
|---|---|
| **II.2** Фильтры колоды · **III.2** Фильтры ленты | Настройка перед путём, а не шаг пути: узлов в потоке M1 нет |
| **IV.1** Список чатов | В потоке M1 не встречается: диалог открывается из совпадения и из подтверждённого отклика |
| **III.4** Создание запроса · **III.5** Отклики на мой запрос · **III.6** Мои планы | Сторона автора — поток R5/S2, шаг 8. В M1 они есть только в ветке «создать свой», которой ищущий пользуется, когда ленты нет |
| **V.1** Напоминание · **V.2** Кто дошёл · **V.3** Предложение повторить · **V.4** Ссылка близкому | После назначенного дня: R3, R4, S3 — шаг 8 |
| **VI.1–VI.4** Жалоба · Блокировка · Заблокированные · Safety center | Поток E3 и §7.4 — шаг 8. На главном пути безопасность держат верификация (I.5) и сигналы доверия на карточке |
| **VII.2–VII.7** Вход, профиль, интересы, интент, геолокация | Одноразовый онбординг до первого глобального экрана; в потоке M1 он позади старта |
| **I.2** Мой профиль · **I.3** Редактирование · **I.4** Что поднимет уровень | Поток S1 (trust loop) — шаг 8 |

---

## 4. Что осталось открытым — решить при вайрфреймах

1. ~~**У профиля (I.1) не описано ни одного состояния**~~ — **решено 23.09:** три частичных
   состояния в `sitemap.md`. То же правило применено к карточке запроса: загрузка там, где
   данных нет на руках (холодное открытие из пуша), и нет там, где экран открывается
   из уже загруженного списка.
2. ~~**У диалога (IV.2) нет загрузки истории**~~ — **решено 23.09:** состояние описано
   и ограничено входом из списка чатов и из подтверждённого отклика.
3. ~~**«Ожидание чужого решения» — пятое состояние**~~ — **решено 23.09, `D-42`:** класс
   назван, правило записано в §6 брифа, пять мест помечены в дереве словом `ОЖИДАНИЕ`.
   Осталось частное: **у двух ожиданий из четырёх не задан срок** (см. таблицу выше) —
   это решается копирайтом на вайрфреймах.
4. ~~**Расхождение в `jtbd.md` по G5**~~ — **сведено 23.09.** Заголовок раздела и цитата
   оставались от версии до 15.09 («Не ждать, когда уже решил»), хотя по итогам В-2 job
   переформулирован в «Доказать, что я — это я». Раздел приведён к матрице, прежняя
   формулировка сохранена в нём как «было». В таблице выше — действующая.
